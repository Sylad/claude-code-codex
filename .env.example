# Required for the weekly batch update GitHub Action.
# Copy to .env (gitignored) for local script runs, or set as repo secrets in CI.

# Anthropic API key — used to summarize new entries via Claude Sonnet
ANTHROPIC_API_KEY=

# Optional — YouTube Data API v3, used to enrich /videos page
YOUTUBE_API_KEY=
